package v5;

// mit klassen arbeiten deren typ man nicht kennt
// absolute pfade in software
// klassen mit belibiegen typen
// generische klassen/typen werden oft verwendet (arrayslist)
// <> diamond paramater , es ist klar was reinkommt
// überall wo vorher t stand steht zur compilation time object?
// generische Methoden
// in der klausur kommt man nicht drum rum
// generisches Flaschenlager bauen man hat objekte weinflaschen und bierflaschen
public class Main {
	/*
	private static SerializationHelper<Rechnung> helper = new SerializationHelper<>(
			"E:\\moin.test");

	public static void main(String[] args) throws IOException {
		Rechnung r = new Rechnung();
		r.Betrag = 10.99;
		r.setKundennummer(123);
		helper.Save(r);
		oos.writeObject(r);

		System.out.println(r.getKundennummer() + ": " + r.Betrag);
		readFile();

	}

	static void readFile() throws IOException, ClassNotFoundException {

		Rechnung inputRechnung = helper.Load(); // er denkt sich das wird
												// rechnung sein

		System.out.println("gelesen: " + inputRechnung.Betrag + " - "
				+ inputRechnung.getKundennummer());
	}
*/
}
